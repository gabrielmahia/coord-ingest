# Contributing

Feedback and new feed adapters welcome via GitHub Issues. Each adapter should
be public-source, key-free where possible, and East-Africa relevant. See the
FeedAdapter base class — implement `fetch()` and `_record_to_event()`.
